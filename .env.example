# Environment variables for PocketBase CDN

# PocketBase Configuration
POCKETBASE_URL=http://localhost:9111

# Flask Configuration (if using Flask frontend)
FLASK_PORT=911
FLASK_HOST=0.0.0.0

# File Upload Configuration
MAX_FILE_SIZE=50MB

# CORS Configuration (comma separated)
ALLOWED_ORIGINS=http://localhost:911,http://localhost:9111

# PocketBase Admin Credentials (for initial setup, change after first login)
# We recommend changing these in the PocketBase dashboard and removing them from this file for security
POCKETBASE_SUPERUSER_EMAIL=admin@example.com
POCKETBASE_SUPERUSER_PASSWORD=admin123

# Note: Most configuration is done in PocketBase admin panel
# This file is for reference only
